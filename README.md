# bikecream

The event page for a long, social ice cream bike crawl through Manhattan, Queens, and Brooklyn.

## Local development

Open `index.html` directly, or run any small static file server from the project directory.

## Validation and deployment build

```sh
npm test
```

The build produces a self-contained Cloudflare Worker in `dist/server/index.js` for deployment through OpenAI Sites.
